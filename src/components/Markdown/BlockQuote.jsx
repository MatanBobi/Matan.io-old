import styled from "styled-components"

export const BlockQuote = styled.blockquote`
    color: var(--colors-primary);
    font-style: italic;
    background: var(--colors-note-background);
    padding: 15px;
    border-radius: 10px;
    transition: all 250ms ease-in-out;
    margin-left: auto;
    margin-right: auto;
    border-left: none;
`
